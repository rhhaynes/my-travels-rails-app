<div class="form-container">

  <div class="form-header-container">
    <h1>Sign in to MyTravels</h1>
  </div>

  <% if @user.errors.any? %>
    <%= render :partial => "shared/form_errors", :locals => {:obj => @user} %>
  <% end %>

  <div class="form-body-container">
    <%= form_for(@user, :url => sessions_path, :method => "post") do |f| %>

      <%= f.label :username, "Username or Email", :class => "form-label" %>
      <%= f.text_field :username, :class => "form-field" %>

      <%= f.label :password, "Password", :class => "form-label" %>
      <%= f.password_field :password, :class => "form-field" %>

      <%= f.submit "Sign in", :class => "form-submit-btn" %>
    <% end %>
  </div>

  <div class="form-divider-container">
    <p>or</p>
  </div>

  <div class="form-body-container">
    <%= button_to("Sign in with Github", "/auth/github", {:method => :get, :class => "form-github-login-btn"}) %>
  </div>

</div>
